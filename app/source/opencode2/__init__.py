"""opencode2 連携パッケージ。

opencode2 のバックグラウンドサーバー（ローカル HTTP API）から
セッション単位の状態を取得するためのモジュール群。

PTY 出力の文言マッチでは「非アクティブタブの状態」が原理的に取れず、
セッションを一意に同定することもできない。API 経由なら各状態に
``sessionID`` が付くため、複数セッションを混同せずに扱える。
"""
